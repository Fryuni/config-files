{
  # GitHub registration scope -> credential name.
  # Personal repositories need one entry each; organizations need only one.
  # Reuse a credential name for repositories covered by the same PAT.
  # Credentials live in secrets/loem/github-actions-<credential>-token.
  # "Fryuni/t3code" = "fryuni";
  # "fryuni-testorg" = "fryuni-testorg";
}
